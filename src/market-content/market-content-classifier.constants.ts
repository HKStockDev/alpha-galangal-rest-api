/**
 * Events/news LLM classifier (Linear CON-83 parent, CON-84 system + CON-85 user on one row).
 * CON-53 should load the active prompt_version by this key—never duplicate prompt text in code.
 */
export const MARKET_CONTENT_CLASSIFIER_FORMULA_KEY = 'market_content_classifier' as const;
